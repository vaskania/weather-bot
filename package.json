{
  "name": "holiday-bot",
  "version": "1.0.0",
  "description": "vaskaniaHolidaybot",
  "main": "app.js",
  "scripts": {
    "dev": "nodemon app.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js",
    "format": "prettier --write . && eslint --fix .",
    "lint": "prettier --check . && eslint .",
    "prepare": "husky install"
  },
  "lint-staged": {
    "*.js": [
      "prettier --write",
      "eslint  --fix"
    ]
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^8.3.0",
    "eslint-config-airbnb-base": "^15.0.0",
    "eslint-plugin-import": "^2.25.3",
    "husky": "^7.0.4",
    "lint-staged": "^12.1.2",
    "prettier": "^2.5.0"
  },
  "dependencies": {
    "axios": "^0.24.0",
    "node-telegram-bot-api": "^0.54.0",
    "pino": "^7.5.1",
    "pino-pretty": "^7.2.0"
  }
}
